apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cert-manager

resources:
  - namespace.yaml
  - ca/ca-issuer.yaml
  - issuers/internal-ca-issuer.yaml
  # Uncomment for Let's Encrypt support:
  # - issuers/letsencrypt-staging.yaml
  # - issuers/letsencrypt-prod.yaml

# Helm chart is installed via Makefile, not kustomize
# These resources are post-install configuration
