apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: external-secrets

resources:
  - namespace.yaml
  - cluster-secret-store.yaml
  # Note: vault-token-secret.yaml is applied manually (contains sensitive data)

commonLabels:
  app.kubernetes.io/managed-by: kustomize
  platform.kubernetes.io/layer: l1
