# NOTE: Update this with your Pi-hole password before deploying
# To create the base64-encoded password:
#   echo -n 'your-password-here' | base64
apiVersion: v1
kind: Secret
metadata:
  name: pihole-password
  namespace: external-dns
type: Opaque
data:
  EXTERNAL_DNS_PIHOLE_PASSWORD: bm9tME9pdV8=  # Replace with: echo -n 'your-password' | base64
